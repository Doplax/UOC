# FP067 — Front End con Frameworks móviles
## Banco de preguntas de exámenes de años anteriores

> Recopilación de las preguntas de la prueba de síntesis disponible (TF6095425).
> Rellena cada **Respuesta:** y pásame el documento para corregirlo.
>
> **Estructura del examen** (4 preguntas × 2,5 p, 30 min, mínimo 5/10, sin material). La asignatura cubre:
> - **Producto 1:** Angular (componentes, directivas `*ngFor`/`*ngIf`, estructura de ficheros, pipes, Bootstrap).
> - **Producto 2:** Firebase / Firestore (persistencia, `environment.ts`, `@angular/fire`).
> - **Producto 3:** React Native (interfaz móvil, FlatList, navegación).
> - **Producto 4:** Notificaciones push (Cloud Messaging, Cloud Functions, `onWrite()`/`onUpdate()`).

---

## Preguntas del examen TF6095425

**1.** Dada la siguiente estructura de ficheros y carpetas trabajada en el Producto 1, indica brevemente para qué sirve cada uno: `main.ts`, `app.routing.module.ts`, `detailItem.component.html`. *(2,5 p — ~2 líneas por fila)*

| Carpeta o archivo | Descripción |
|---|---|
| `main.ts` | |
| `app.routing.module.ts` | |
| `detailItem.component.html` | |

Respuesta:


**2.** En el siguiente código de los Productos 1 y 2, ¿qué error existe para que no funcione correctamente? *(2,5 p — ~3 líneas)*
```html
<h2>Lista</h2>
<ul>
  <li *ngFor="let items from item">
    <span class="badge"> {{item.id}}</span>{{item.name}}
  </li>
</ul>
```
Respuesta:


**3.** Explica para qué sirven los eventos `onWrite()` y `onUpdate()` de Cloud Messaging trabajados en el Producto 4. *(2,5 p — ~6 líneas)*

Respuesta:


**4.** Explica resumidamente para qué se utiliza el fichero `environment.ts` y su relación con la conexión con Firebase/Firestore trabajada en el Producto 2. *(2,5 p — ~5 líneas)*

Respuesta:
